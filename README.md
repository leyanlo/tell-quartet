# Tell Quartet

Landing page for Tell Quartet, a San Francisco Bay Area string quartet.

## Local development

```sh
npm install
npm run dev
```

## Build

```sh
npm run build
```

The static site is generated in `dist/` and configured for Netlify in `netlify.toml`.
